<SettingsFlyout
    x:Class="Kozlowski.Slideshow.SlideshowSettingsFlyout"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Kozlowski.Slideshow"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    IconSource="Assets/SmallLogo.png"
    Title="Slide"
    d:DesignWidth="346">

    <StackPanel VerticalAlignment="Stretch" HorizontalAlignment="Stretch" >
        
        <!-- Interval dropdown -->
        <StackPanel Style="{StaticResource SettingsFlyoutSectionStyle}">
            <TextBlock Text="Interval" Style="{StaticResource TitleTextBlockStyle}"/>
            <ComboBox x:ConnectionId='3' Name="Interval" Margin="0,7,0,0" HorizontalAlignment="Left"                                     PlaceholderText="Select Interval" SelectedIndex="{Binding Path=Index, Mode=TwoWay}">
                <x:String>5 seconds</x:String>
                <x:String>10 seconds</x:String>
                <x:String>30 seconds</x:String>
                <x:String>1 minute</x:String>
                <x:String>3 minutes</x:String>
            </ComboBox>
        </StackPanel>
        
        <!-- Root Folder -->
        <StackPanel Style="{StaticResource SettingsFlyoutSectionStyle}">
            <TextBlock Text="Picture Location" Style="{StaticResource TitleTextBlockStyle}"/>
            <StackPanel Margin="0,7,0,0" Orientation="Horizontal">
                <TextBox Header="Root Folder" HorizontalAlignment="Left" Width="150" Text="{Binding Path=FolderPath, Mode=TwoWay}"/>
                <Button x:ConnectionId='2' x:Name="Browse" Content="Browse" Margin="20,0,0,0"                      VerticalAlignment="Bottom"/>
            </StackPanel>
            <ToggleSwitch x:ConnectionId='1' Name="SubfoldersSwitch" Header="Include Sub-Folders" IsOn="{Binding Path=IncludeSubfolders, Mode=TwoWay}"                                />
        </StackPanel>
        
    </StackPanel>
</SettingsFlyout>

